[build-system]
build-backend = "uv_build"
requires = [ "uv-build>=0.9.21,<0.10" ]

[project]
name = "fedotmas-llm"
version = "0.1.0"
description = "LLM Primitives and Serving for Agent Systems"
readme = "README.md"
authors = [ { name = "Dmitry Gilemkhanov", email = "me@dmitryglhf.ru" } ]
requires-python = ">=3.11"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "fedotmas",
]
optional-dependencies.pydantic-ai = [
  "pydantic-ai-slim[mcp,openai,openrouter]>=1.106",
  # pinned directly: some pydantic-ai-slim releases (2.43.0 seen in practice) import httpx
  # from pydantic_ai.mcp but declare httpx2 as their own dependency instead, so httpx is
  # not pulled in transitively without this.
  "httpx>=0.27",
]
optional-dependencies.serve = [
  "dishka>=1.10.1",
  "fastapi[standard]>=0.137.2",
  "fastmcp>=3.4.2",
  "redis>=8",
]

[tool.uv]
sources.fedotmas = { workspace = true }
